#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @k: the integer to be computed
 * Return: the absolute value
 */
int _abs(int k)
{
	if (k >= 0)
		return (k);
	else
		return (-k);
}
